implements Iterable<Text>
implements Comparable<Text>

// JsonElement toJson()

Component component()

String getString()

itself copy()

// JsonObject createStyleJson()

boolean hasStyle()

Style createStyle()

readonly itself color(Color c)

readonly itself black()

readonly itself darkBlue()

readonly itself darkGreen()

readonly itself darkAqua()

readonly itself darkRed()

readonly itself darkPurple()

readonly itself gold()

readonly itself gray()

readonly itself darkGray()

readonly itself blue()

readonly itself green()

readonly itself aqua()

readonly itself red()

readonly itself lightPurple()

readonly itself yellow()

readonly itself white()

readonly itself noColor()

readonly itself bold(nullable Boolean value)

readonly itself bold()

readonly itself italic(nullable Boolean value)

readonly itself italic()

readonly itself underlined(nullable Boolean value)

readonly itself underlined()

readonly itself strikethrough(nullable Boolean value)

readonly itself strikethrough()

readonly itself obfuscated(nullable Boolean value)

readonly itself obfuscated()

readonly itself insertion(nullable String value)

readonly itself font(nullable String value)

readonly itself click(nullable String value)

readonly itself hover(Object text)

readonly itself append(Object sibling)

readonly List<Text> getSiblings()

readonly boolean hasSiblings()